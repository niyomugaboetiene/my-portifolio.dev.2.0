{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd my-portifolio && npm install && npm run build",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "nodemailer": "^6.9.8",
    "sitemap": "^8.0.0"
  }
}
